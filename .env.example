# MySQL 接続情報（Docker 利用時はサービス名 db を host にする）
# MYSQL_USER / MYSQL_PASSWORD / MYSQL_DATABASE は必須。未設定だと起動時に失敗する
# （弱い既定資格情報での意図せぬ接続を防ぐ fail-fast）。HOST/PORT は未設定なら
# ローカル既定値（127.0.0.1:3306）になる。
MYSQL_HOST=db
MYSQL_PORT=3306
MYSQL_USER=app
MYSQL_PASSWORD=app_password
MYSQL_DATABASE=app_db

# ログ出力レベル（DEBUG / INFO / WARNING / ERROR）。未設定時は INFO。
LOG_LEVEL=INFO
